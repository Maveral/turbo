<%= form_with(model: mechanic) do |form| %>
  <% if mechanic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mechanic.errors.count, "error") %> prohibited this mechanic from being saved:</h2>

      <ul>
        <% mechanic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :surname %>
    <%= form.text_field :surname %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.date_select :age %>
  </div>

  <div class="field">
    <%= form.label :experience %>
    <%= form.number_field :experience, in: 1...11 %>
  </div>

  <div class="field">
    <%= form.label :rider_id %>
    <%= form.collection_select :rider_id, Rider.all, :id, :surename %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
